de:
  api_key:
    invalid: Der übergebene API-Key ist ungültig.
    missing: Es wurde kein API-Key übergeben.
    no_permision: Mit dem übergebenen API-Key stehen die benötigten Zugriffsrechte nicht zur Verfügung.

  backend:
    unavailable: Das Backend ist momentan nicht erreichbar.

  geocoding:
    unavailable: Die API für die Geokodierung ist momentan nicht erreichbar.
    blank_response: Für die Adresse '%{address}' konnten keine Koordinaten ermittelt werden.
    invalid_address: Das Format der Adresse '%{address}' kann nicht zur Koordinatenermittlung verwendet werden.

  activemodel:
    errors:
      models:
        request:
          attributes:
            description:
              blank: Es wurde keine Beschreibung übergeben.
            detailed_status:
              forbidden: Der übergebene Status ist für diese Meldung nicht erlaubt.
            position:
              invalid: Es konnte keine gültige Position ermittelt werden.
            service_code:
              blank: Es wurde keine Kategorie übergeben.
            title:
              blank: Es wurde kein Titel übergeben.
