de:
  api_key:
    invalid: Der übergebene API-Key ist ungültig.
    missing: Es wurde kein API-Key übertragen.
    no_permision: Der API-Key hat nicht die benötigten Zugriffsrechte.

  backend:
    unavailable: Klarschiff-Backend ist Momentan nicht erreichbar.

  geocoding:
    unavailable: Geokodierung ist Momentan nicht erreichbar.
    blank_response: Koordinaten konnten nicht für die Adresse '%{address}' ermittelt werden.
    invalid_address: Das Format der Adresse '%{address}' kann nicht zur Koordinatenermittung verwendet werden.

  activemodel:
    errors:
      models:
        request:
          attributes:
            description:
              blank: Es wurde keine Beschreibung übergeben
            detailed_status:
              forbidden: Der übergebene Status ist für diesen Vorgang nicht erlaubt
            position:
              invalid: Es konnte keine gültige Position ermittelt werden
            service_code:
              blank: Es wurde keine Kategorie übergeben
            title:
              blank: Es wurde kein Titel übergeben
